/** @type {import('tailwindcss').Config} */
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}",
  ],
  theme: {
    extend: {
      animation: {
        'scroll-left-smooth': 'scrollLeftSmooth 12s linear infinite', 
        'bounce': 'bounce 2s infinite', 
      },
      keyframes: {
        scrollLeftSmooth: {
          '0%': { transform: 'translateX(100%)' },
          '100%': { transform: 'translateX(-100%)' },
        },
      },
    },
  },
  plugins: [],
};
